export function LoginPage() {
  return (
    <section aria-labelledby="login-page-title">
      <span>Public route</span>

      <h1 id="login-page-title">Authentication entry point</h1>

      <p>
        The login form will be implemented in the next block on top of the routing,
        layout and session foundation already in place.
      </p>
    </section>
  )
}
